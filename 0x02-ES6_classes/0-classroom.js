// Exporting the ClassRoom class as the default export
export default class ClassRoom {
  // Constructor to initialize the maximum student size
  constructor(maxStudentSize) {
    // Setting the maximum number of students allowed in the classroom
    this._maxStudentsSize = maxStudentSize;
  }
}
